import React from "react";
export default function PalavraRevertida(props) {
    return (
        <section>
            <p>Sua palavra revertida = {props.palavraRevertida}</p>
        </section>
    );
}
